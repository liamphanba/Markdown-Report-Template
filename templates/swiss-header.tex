% Minimal Swiss report style for XeLaTeX export.
% Colours (ink, muted, heading, posterbg, posterfg, backpagebg) are defined
% in report.tex template via YAML variables — do not redefine them here.

\color{ink}

% Math formulas are handled by Pandoc's unicode-math pipeline (XeLaTeX).
\usepackage{textcomp}

% No headers; only page number at bottom-right.
\usepackage{fancyhdr}
\usepackage{tocloft}
\pagestyle{fancy}
\fancyhf{}
\setlength{\footskip}{\ReportFooterSkip}
\fancyfoot[R]{\color{ink}\ReportFooterFontSize\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
% Chapter-opening pages also use plain style — keep consistent
\fancypagestyle{plain}{\fancyhf{}\setlength{\footskip}{\ReportFooterPlainSkip}\fancyfoot[R]{\color{ink}\ReportFooterPlainFontSize\thepage}\renewcommand{\headrulewidth}{0pt}\renewcommand{\footrulewidth}{0pt}}
% Customize TOC: no dots, keep page numbers
\renewcommand{\cftdot}{}
\renewcommand{\cftchappagefont}{\normalfont}

% Clean hierarchy, left aligned, no separator lines.
\setcounter{secnumdepth}{-1}
\setcounter{tocdepth}{3}
\usepackage{titlesec}

% Treat chapters as straight headings and never force double-page breaks.
\titleclass{\chapter}{straight}
\let\cleardoublepage\clearpage

\titleformat{\chapter}[display]
  {\raggedright\normalfont\bfseries\color{heading}}
  {}
  {0pt}
  {\fontsize{\ReportHOneSize}{\ReportHOneLeading}\selectfont}

\titlespacing*{\chapter}{0pt}{0pt}{\ReportHOneAfter}

\titleformat{\section}[block]
  {\raggedright\normalfont\bfseries\fontsize{\ReportHTwoSize}{\ReportHTwoLeading}\selectfont\color{heading}}
  {\thesection}
  {10pt}
  {}

\titlespacing*{\section}{0pt}{\ReportHTwoBefore}{\ReportHTwoAfter}

\titleformat{\subsection}[block]
  {\raggedright\normalfont\bfseries\fontsize{\ReportHThreeSize}{\ReportHThreeLeading}\selectfont\color{heading}}
  {\thesubsection}
  {8pt}
  {}

\titlespacing*{\subsection}{0pt}{\ReportHThreeBefore}{\ReportHThreeAfter}

\titleformat{\subsubsection}[runin]
  {\normalfont\bfseries\fontsize{\ReportHFourSize}{\ReportHFourLeading}\selectfont\color{heading}}
  {\thesubsubsection}
  {6pt}
  {}
  [.] 

\titlespacing*{\subsubsection}{0pt}{\ReportHFourBefore}{\ReportHFourAfter}

% Body text: tighter spacing to minimize blank pages
\setlength{\parindent}{0pt}
\setlength{\parskip}{\ReportParSkip}
\raggedbottom

% Simple quote style without decorative rules.
\renewenvironment{quote}
  {\list{} {\leftmargin=1.2em\rightmargin=1.2em}\item\relax\itshape\color{muted}}
  {\endlist}

% Hyperlink colors, guarded if hyperref is missing.
\providecommand{\hypersetup}[1]{}
\hypersetup{colorlinks=true,urlcolor=ink,linkcolor=ink,citecolor=ink}

% Title page and TOC have no footer; content starts at page 1 after the TOC.
\AtBeginDocument{%
  \pagenumbering{gobble}%
  \let\frontmatter\relax%
  \let\mainmatter\relax%
  \providecommand{\horizontalrule}{}%
  \renewcommand{\horizontalrule}{}%
  \pagestyle{empty}%
}
