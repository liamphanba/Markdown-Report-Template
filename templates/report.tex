$document-metadata.latex()$
$passoptions.latex()$
\documentclass[
$for(babel-otherlangs)$
  $babel-otherlangs$,
$endfor$
$if(babel-lang)$
  $babel-lang$,
$endif$
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}
$if(beamerarticle)$
\usepackage{beamerarticle}
$endif$
\usepackage{xcolor}
% ── Theme colours ─────────────────────────────────────────────────────────────
% Override any of these in report.md YAML front matter:
%   color-ink, color-muted, color-heading,
%   color-front-bg, color-front-fg, color-back-bg
\definecolor{ink}{HTML}{$if(color-ink)$$color-ink$$else$111111$endif$}
\definecolor{muted}{HTML}{$if(color-muted)$$color-muted$$else$545454$endif$}
\colorlet{heading}{ink}
$if(color-heading)$\definecolor{heading}{HTML}{$color-heading$}$endif$
\definecolor{posterbg}{HTML}{$if(color-front-bg)$$color-front-bg$$else$111111$endif$}
\definecolor{posterfg}{HTML}{$if(color-front-fg)$$color-front-fg$$else$FFFFFF$endif$}
\definecolor{backpagebg}{HTML}{$if(color-back-bg)$$color-back-bg$$else$D9D9D9$endif$}
% ──────────────────────────────────────────────────────────────────────────────
$if(geometry)$
\usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}
$endif$
\usepackage{amsmath,amssymb}
$if(cancel)$
\usepackage{cancel}
$endif$
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$
$fonts.latex()$
$font-settings.latex()$
$common.latex()$
% ── Style tokens from metadata ───────────────────────────────────────────────
% Configure these in report-config.yaml.
\newcommand{\ReportFooterFontSize}{$if(style-footer-fontsize)$$style-footer-fontsize$$else$\normalsize$endif$}
\newcommand{\ReportFooterSkip}{$if(style-footer-footskip)$$style-footer-footskip$$else$100pt$endif$}
\newcommand{\ReportFooterPlainFontSize}{$if(style-footer-plain-fontsize)$$style-footer-plain-fontsize$$else$\Large$endif$}
\newcommand{\ReportFooterPlainSkip}{$if(style-footer-plain-footskip)$$style-footer-plain-footskip$$else$90pt$endif$}

\newcommand{\ReportHOneSize}{$if(style-h1-size)$$style-h1-size$$else$40pt$endif$}
\newcommand{\ReportHOneLeading}{$if(style-h1-leading)$$style-h1-leading$$else$40pt$endif$}
\newcommand{\ReportHOneAfter}{$if(style-h1-after)$$style-h1-after$$else$24pt$endif$}

\newcommand{\ReportHTwoSize}{$if(style-h2-size)$$style-h2-size$$else$18pt$endif$}
\newcommand{\ReportHTwoLeading}{$if(style-h2-leading)$$style-h2-leading$$else$22pt$endif$}
\newcommand{\ReportHTwoBefore}{$if(style-h2-before)$$style-h2-before$$else$18pt$endif$}
\newcommand{\ReportHTwoAfter}{$if(style-h2-after)$$style-h2-after$$else$10pt$endif$}

\newcommand{\ReportHThreeSize}{$if(style-h3-size)$$style-h3-size$$else$13pt$endif$}
\newcommand{\ReportHThreeLeading}{$if(style-h3-leading)$$style-h3-leading$$else$16pt$endif$}
\newcommand{\ReportHThreeBefore}{$if(style-h3-before)$$style-h3-before$$else$14pt$endif$}
\newcommand{\ReportHThreeAfter}{$if(style-h3-after)$$style-h3-after$$else$8pt$endif$}

\newcommand{\ReportHFourSize}{$if(style-h4-size)$$style-h4-size$$else$11pt$endif$}
\newcommand{\ReportHFourLeading}{$if(style-h4-leading)$$style-h4-leading$$else$13pt$endif$}
\newcommand{\ReportHFourBefore}{$if(style-h4-before)$$style-h4-before$$else$10pt$endif$}
\newcommand{\ReportHFourAfter}{$if(style-h4-after)$$style-h4-after$$else$8pt$endif$}

\newcommand{\ReportParSkip}{$if(style-parskip)$$style-parskip$$else$6pt plus 1pt minus 0.5pt$endif$}
% ─────────────────────────────────────────────────────────────────────────────
$for(header-includes)$
$header-includes$
$endfor$
$after-header-includes.latex()$
$hypersetup.latex()$

% ── Cover page commands ───────────────────────────────────────────────────────
% Pandoc template variables are substituted here, before XeLaTeX compilation.
\renewcommand{\maketitle}{%
  \begin{titlepage}%
    \pagecolor{posterbg}%
    \color{posterfg}%
    \thispagestyle{empty}%
    \raggedright
    \vspace*{0.10\paperheight}
    {\fontsize{56pt}{56pt}\selectfont\bfseries $if(fronttitle)$$fronttitle$$else$$title$$endif$ \par}
$if(frontsubtitle)$
    \vspace{12pt}
    {\fontsize{18pt}{22pt}\selectfont $frontsubtitle$ \par}
$elseif(subtitle)$
    \vspace{12pt}
    {\fontsize{18pt}{22pt}\selectfont $subtitle$ \par}
$endif$
    \vfill
    {\fontsize{12pt}{14pt}\selectfont $if(frontauthor)$$frontauthor$$else$$for(author)$$author$$sep$, $endfor$$endif$ \par}
    {\fontsize{11pt}{13pt}\selectfont $if(frontdate)$$frontdate$$else$$date$$endif$ \par}
  \end{titlepage}%
  \nopagecolor
  \color{ink}%
}

\newcommand{\makebackpage}{%
  \begin{titlepage}%
    \pagecolor{backpagebg}%
    \color{ink}%
    \thispagestyle{empty}%
    \raggedright
    \vspace*{0.10\paperheight}
$if(backtitle)$
  {\fontsize{56pt}{56pt}\selectfont\bfseries $backtitle$ \par}
$endif$
$if(backsubtitle)$
  \vspace{12pt}
  {\fontsize{18pt}{22pt}\selectfont $backsubtitle$ \par}
$endif$
    \vfill
$if(backauthor)$
  {\fontsize{12pt}{14pt}\selectfont $backauthor$ \par}
$endif$
$if(backdate)$
  {\fontsize{11pt}{13pt}\selectfont $backdate$ \par}
$endif$
  \end{titlepage}%
  \nopagecolor
  \color{ink}%
}
% ─────────────────────────────────────────────────────────────────────────────

$if(title)$
\title{$title$}
$endif$
\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$date$}

\begin{document}
$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
\maketitle
$endif$

$for(include-before)$
$include-before$

$endfor$
$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
{
$if(colorlinks)$
$if(toccolor)$
\hypersetup{linkcolor=$toccolor$}
$endif$
$endif$
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$
$if(has-frontmatter)$
\mainmatter
$endif$
$body$

$if(has-frontmatter)$
\backmatter
$endif$
$for(include-after)$
$include-after$

$endfor$
\end{document}
